package com.quispe.anota_ai_challenge.services.aws;
/*
 * Quando precisa de classes simples que só armazenam dados.
 * Para criar DTOs (como no seu exemplo).
 * Sempre que quiser um código mais limpo e menos verboso.
 */
public record MessageDTO(String message) {
    
}
